{
  "name": "no-cors-proxy-resolver",
  "version": "0.0.1",
  "description": "CORS Anywhere is a reverse proxy which adds CORS headers to the proxied request. Request URL is taken from the path",
  "license": "MIT",
  "author": "Olavo Parno <olavoparno@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/olavoparno/no-cors-proxy-resolver.git"
  },
  "bugs": {
    "url": "https://github.com/olavoparno/no-cors-proxy-resolver/issues/",
    "email": "olavoparno@gmail.com"
  },
  "keywords": [
    "cors",
    "cross-domain",
    "http-proxy",
    "proxy",
    "heroku"
  ],
  "dependencies": {
    "http-proxy": "1.18.1",
    "proxy-from-env": "1.1.0"
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "eslint": "^7.1.0",
    "istanbul": "^0.4.5",
    "lolex": "^6.0.0",
    "mocha": "^7.2.0",
    "nock": "^12.0.3",
    "supertest": "^4.0.2"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "mocha ./test/test*.js --reporter spec",
    "test-coverage": "istanbul cover ./node_modules/.bin/_mocha -- test/test.js test/test-ratelimit.js --reporter spec"
  }
}
